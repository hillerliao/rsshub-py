version: '3'

services:
  rsshub-py:
    build: .
    container_name: rsshub-py
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - HOST=0.0.0.0
      - PORT=5000
    volumes:
      - ./cache:/app/cache
    restart: unless-stopped